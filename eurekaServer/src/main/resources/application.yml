server:
  port: 8761

spring:
  application:
    name: eureka-server

#스프링 유레카 서버 구축
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
        default-zone: http://localhost:8761/eureka/
